import React from "react";

import drawerActions from "src/Drawer/redux/drawer/actions";

import { Drawer } from "antd"


export default function CraftAds() {
  return (<div></div>
    // <Drawer
    //   placement="right"
    //   closable={ true }
    //   onClose={ () => toggleDrawer(false) }
    //   visible={ drawerVisibility }
    //   width={ 700 }
    //   destroyOnClose={ true }
    // />
  )
}